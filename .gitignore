# ── 合并仓库根级忽略 ─────────────────────────────────────
#
# upstream/ 与 manager/ 各自保留原仓库的 .gitignore，会继续在各自目录内生效
# （data/、config.json、node_modules、.venv、web/out、*.db …）。
# 这里只补「合并后才出现」的顶层条目。

# 统一环境变量：只提交模板，不提交填好的那份
.env
.env.local
.env.*.local
# .env.example 必须进版本库（部署时的唯一参照），它本身匹配上面的 .env.*
!.env.example

# 顶层数据目录（若按 README 的「集中数据布局」把卷收到这里）
/data/
/config.json
web/out/
.venv/
__pycache__/
**/__pycache__/
*.py[cod]
*.exe
*.pid

# 运行期日志 / 锁 / 状态
*.log
.update.lock

# 本地备份（迁移或实验前的快照）
/backup/
*.bak
*.orig

# 编辑器 / 系统
.idea/
.vscode/
.DS_Store
Thumbs.db
