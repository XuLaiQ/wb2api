# 行尾规范化
#
# 为什么必须有：本仓库同时装着 Windows 批处理（.cmd）和 Linux shell 脚本（.sh），
# 两者对行尾的要求正好相反 ——
#   · cmd.exe 对 CRLF 最稳（纯 LF 在标签跳转、多行块等场景可能解析异常）；
#   · Linux 的 shebang 碰到 CRLF 会直接报 `bad interpreter: /bin/bash^M`，
#     脚本一行都跑不了。
#
# 不钉死的话，行尾取决于每个人的 core.autocrlf，于是「在我机器上好好的」变成
# 「clone 到服务器就起不来」——而且报错信息（^M）跟真实原因看起来毫无关系。
#
# 上游 Dockerfile 里有 `sed -i 's/\r$//' /app/*.sh` 兜底，但那只覆盖容器内；
# 原生 Linux 部署（直接跑 login.sh / checkin.sh）仍然依赖这里。

# 默认：文本文件统一用 LF
* text=auto eol=lf

# Windows 批处理 / PowerShell：检出为 CRLF
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf

# Shell 脚本：必须是 LF（显式写出来，避免将来有人把默认值改掉）
*.sh text eol=lf

# 二进制：不做任何行尾转换
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.webp binary
*.woff  binary
*.woff2 binary
*.ttf  binary
*.eot  binary
*.exe  binary
*.db   binary
*.gz   binary
